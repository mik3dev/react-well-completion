# Tasks

- [x] 1. Calcular `halfSecPanelFill`, `effectiveTrackWidth`, `panelWidth` en `WellDiagram.tsx`
- [x] 2. Ajustar `configW` para mantener ancho completo en modo half-sec fill
- [x] 3. Calcular `panelOffsetX` según `halfSide` para el panel vertical
- [x] 4. Smoke tests: half-sec + halfSide='right' y half-sec + halfSide='left' con verificación de track width
- [x] 5. Helper `withFakeContainerSize` en smoke.test.tsx para tests que necesitan dimensiones reales
- [x] 6. Actualizar READMEs (lib + root) con nota sobre half-section + panel fill
