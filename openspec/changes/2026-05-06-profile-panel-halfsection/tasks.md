# Tasks

- [x] 1. Calcular `halfSecPanelFillV`, `halfSecPanelFillH`, `effectiveTrackWidth`, `effectiveTrackHeightH`, `panelWidth`, `panelHeight` en `WellDiagram.tsx`
- [x] 2. Ajustar `configW` para mantener ancho/alto completo en modos half-sec fill (vertical y horizontal)
- [x] 3. Calcular `panelOffsetX` según `halfSide` para el panel vertical
- [x] 4. Calcular `panelOffsetY` según `halfSide` para el panel horizontal
- [x] 5. Smoke tests: half-sec vertical (halfSide='right' y 'left') con verificación de track width
- [x] 6. Smoke tests: half-sec horizontal (halfSide='left' y 'right') con verificación de track height
- [x] 7. Helper `withFakeContainerSize` en smoke.test.tsx para tests que necesitan dimensiones reales
- [x] 8. Actualizar READMEs (lib + root) con nota sobre half-section + panel fill (vertical y horizontal)
