# Tasks

- [x] 1. Extender `SimplifiedDiagramProps` con `profiles?`, `profileLayout?`, `profileTrackWidth?`
- [x] 2. Importar `Profile`, `ProfileLayout`, `ProfilePanel`, `TooltipProvider` en `SimplifiedDiagram.tsx`
- [x] 3. Calcular `panelWidth` (vertical) y `panelHeight` (horizontal); ajustar `configW`
- [x] 4. Wrappear el componente en `TooltipProvider` para soportar tooltips de la curva
- [x] 5. Renderizar el panel vertical (translate al borde derecho)
- [x] 6. Renderizar el panel horizontal (translate debajo del diagrama)
- [x] 7. Smoke tests: vertical render, horizontal render con 2 tracks, regresión sin profiles
- [x] 8. Demo-app: pasar `profiles` a `SimplifiedDiagram` y habilitar el botón Perfiles en modo simplified
- [x] 9. Actualizar READMEs (lib + root)
