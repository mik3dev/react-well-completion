# Tasks

- [x] 1. Tipos públicos `Profile`, `ProfilePoint`, `ProfileLayout` + re-exports
- [x] 2. `sortAndFilterPoints` (sort + filter sin mutar input)
- [x] 3. `buildScale` (auto-min/max con padding 5%, overrides, sanitización)
- [x] 4. `valueToPos` y `getProfileColor` (mapeo lineal + paleta cíclica)
- [x] 5. `formatTooltipValue` (entero limpio, fracción a 2 decimales)
- [x] 6. Componente `ProfileCurve` (polyline + hover dots; soporta vertical y horizontal)
- [x] 7. Componente `ProfileTrack` (header + axis + grid + curva)
- [x] 8. Componente `ProfilePanel` (orquesta N tracks)
- [x] 9. Integrar `ProfilePanel` en `WellDiagram` (vertical)
- [x] 10. Soporte horizontal en `WellDiagram`
- [x] 11. Datos de ejemplo en demo-app + verificación visual
- [x] 12. OpenSpec change (este documento)
